{
  "name": "@holokai/provider-contract-tests",
  "version": "1.2.2",
  "private": true,
  "type": "module",
  "description": "Vitest conformance runners bridging @holokai/test-harness into Vitest assertions",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@holokai/test-harness": "*",
    "@holokai/types": "*"
  },
  "peerDependencies": {
    "vitest": "^3.0.0"
  }
}
